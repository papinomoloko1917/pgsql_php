<?php

declare(strict_types=1);

require __DIR__ . '/vendor/autoload.php';

if ($argv[0] === 'assistant') {
    if ($argv[1] === 'migrate') {
        require_once __DIR__ . '/bin/migration.php';
    }
}
